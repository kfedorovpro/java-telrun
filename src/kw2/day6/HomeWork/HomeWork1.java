package kw2.day6.HomeWork;

/**
 * @author Kostyantyn Fedorov
 * 14.09.2020
 */

/* public class HomeWork1 {
    /* Нам нужно взять первую букву и последнюю.
    Для первого слова мы используем substring от 0 до 1 и узнаем первую букву.
    От второго слова мы в substring закидываем длину слова и забираем последнюю букву.
     */
/*    public static void main(String[] args) {
        System.out.println(lastChars("last", "chars"));
        System.out.println(lastChars("yo", "java"));
        //System.out.println(lastChars("hi", ""));
        //Как-то нужно через if else, но не понимаю, как с return это делать
        char a = '@';

        System.out.println(makeAbba("Hi", "Bye"));
        System.out.println(makeAbba("Yo", "Alice"));
        System.out.println(makeAbba("What", "Up"));

        System.out.println(extraEnd("Hello"));
        System.out.println(extraEnd("ab"));
        System.out.println(extraEnd("Hi"));

    }

    private static String makeAbba(String hao, String bao) {
        String result = hao + bao + bao + hao;
        //очень подозрительно просто?
        return result;
    }

    private static String extraEnd(String e1) {
        String result = e1.substring(e1.length() - 2) + e1.substring(e1.length() - 2) + e1.substring(e1.length() - 2);
        // Беру 2 последних буквы и 3 раза их вставляю через substring и length
        return result;
    }


    private static String lastChars(String s1, String s2, char a) {
        String result = s1.substring(0, 1) + s2.substring(s2.length() - 1);
        return result;
    }


}

 */
